<UserControl x:Class="Profisys_Programming_Task.View.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Profisys_Programming_Task.View"
             xmlns:viewmodel="clr-namespace:Profisys_Programming_Task.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:ExportViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1200">
    <Grid Background="White">
        <Button Height="75" Width="150" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Command="{Binding SwitchToMainMenuCommand}">Back to menu</Button>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label FontSize="16" HorizontalAlignment="Center" Margin="10">Export data to csv files</Label>
            <Button Height="60" Width="120" Margin="10" HorizontalAlignment="Left" Command="{Binding ExportDocumentsCommand}">
                <TextBlock TextWrapping="Wrap">Export documents</TextBlock>
            </Button>
            <TextBlock Text="{Binding DocumentsCountText}"/>
            <Label FontSize="12" HorizontalAlignment="Center" Margin="10"></Label>
            <Button Height="60" Width="120" Margin="10" HorizontalAlignment="Left" Command="{Binding ExportDocumentItemsCommand}">
                <TextBlock TextWrapping="Wrap">Export document items</TextBlock>
            </Button>
            <TextBlock Text="{Binding DocumentItemsCountText}"/>
        </StackPanel>
    </Grid>
</UserControl>
